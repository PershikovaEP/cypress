{
  "name": "cypress-task6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open ",
    "cy:open:env": "npx cypress open --config-file cypress.env.config.js",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:edge": "npx cypress run --browser edge",
    "cy:run:env": "npx cypress run --config-file cypress.env.config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.11.0"
  }
}
