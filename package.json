{
  "name": "cypress-task6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:open:edge": "npx cypress open --browser edge",
    "cy:open:env:chrome": "npx cypress open --config-file cypress.env.config.js --browser chrome",
    "cy:open:env:edge": "npx cypress open --config-file cypress.env.config.js --browser edge",
    "cy:run": "npx cypress run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.11.0"
  }
}
